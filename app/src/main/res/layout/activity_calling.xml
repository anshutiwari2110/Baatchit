<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".VideoConfig.CallingActivity">

    <ImageView
        android:id="@+id/user_profile_calling"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"/>

    <TextView
        android:id="@+id/caller_username"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/txt"
        android:fontFamily="@font/capriola"
        android:gravity="center"
        android:maxLines="5"
        android:layout_marginBottom="20dp"
        android:text="Username"
        android:textSize="24sp">

    </TextView>

    <TextView
        android:id="@+id/txt"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/linear_layout_user"
        android:layout_marginBottom="20dp"
        android:fontFamily="@font/capriola"
        android:gravity="center"
        android:maxLines="5"
        android:text="Calling..."
        android:textSize="18sp">

    </TextView>

    <LinearLayout
        android:id="@+id/linear_layout_user"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="40dp"
        android:gravity="center"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/make_a_call"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:layout_marginEnd="120dp"
            android:background="#00ff00"
            android:padding="15dp"
            android:src="@drawable/ic_call_pick"
            android:visibility="gone">

        </ImageView>

        <ImageView
            android:id="@+id/cancel_a_call"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:background="@drawable/bg_end_call"
            android:padding="15dp"
            android:src="@drawable/ic_call_end">

        </ImageView>
    </LinearLayout>

</RelativeLayout>